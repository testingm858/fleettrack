{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=apps/backend/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/backend/prisma/schema.prisma && npx tsx apps/backend/prisma/seed.ts && npx tsx apps/backend/src/server.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
